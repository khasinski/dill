<% content_for(:title) { "Edit #{ @section.title }" } %>

<% content_for :header do %>
  <%= render "chapterables/edit_header", chapter: @chapter, report: @report %>
<% end %>

<% content_for :toolbar do %>
  <div class="page-toolbar fill-selected align-center gap-half margin-block-end-double">
    <%= editing_mode_toggle_switch(@chapter, checked: true) %>

    <span class="separator margin-inline-half" aria-hidden="true"></span>

    <label class="btn txt-small">
      <%= hidden_field_tag "section[theme]", nil, id: nil, form: "chapterable-editor" %>
      <%= check_box_tag "section[theme]", "dark", @section.theme.present?, class: "switch__input", form: "chapterable-editor" %>
      <%= image_tag "theme-switch.svg", aria: { hidden: true }, size: 24 %>
      <span class="for-screen-reader">Change theme</span>
    </label>

    <span class="separator margin-inline-half" aria-hidden="true"></span>

    <button type="submit" form="chapterable-editor" class="btn flex page-toolbar__save flex-item-justify-end flex-item-no-shrink txt-small">
      <%= image_tag "check.svg", aria: { hidden: true }, size: 24 %>
      <span class="for-screen-reader">Save</span>
    </button>
  </div>
<% end %>

<% content_for :sidebar do %>
  <%= render "chapters/sidebar", report: @report %>
<% end %>

<div class="page--section <%= "theme--dark" if @section.theme == "dark" %>">
  <%= render "sections/form", report: @report, section: @section %>
</div>

<%= render "chapters/edit_footer", chapter: @chapter %>
